# Quarto report requirements — install into the Python that Quarto uses:
#   pip install -r requirements.txt
# Or if using a venv:
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#   QUARTO_PYTHON=.venv/bin/python quarto render report.qmd --to html

pandas>=2.0
plotly>=5.0
ipykernel>=6.0
nbformat>=5.0
nbclient>=0.8
tabulate>=0.9
