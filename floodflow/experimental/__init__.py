"""Experimental analyses kept for future study, not used by the default
pipeline. reachability: filter streams to cells whose flow reaches the
parcel (boolean or flow-weighted). Off by default because D8/D∞ routing on
near-flat fan terrain often misses the parcel (see AGENTS.md)."""
